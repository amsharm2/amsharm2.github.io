# amsharm2.github.io

Personal website / portfolio for Amit — scaffolded with Jekyll and a minimal Materialize CSS layout.

Quick notes:

- Pages added: `index.html`, `projects.md`, `work-profile.md`, `contact.md`
- Layout: `_layouts/default.html` (Materialize CSS via CDN)
- Assets: `assets/css/style.css`

Local preview (optional): GitHub Pages will build the site automatically for a `username.github.io` repo.

If you want to run locally you can install Jekyll (Ruby) and run:

```powershell
gem install bundler jekyll; jekyll serve
```

Then open http://localhost:4000

To deploy: push to the `main` branch of this repo; GitHub Pages will publish the site at `https://amsharm2.github.io/`.

