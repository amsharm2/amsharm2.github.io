source "https://rubygems.org"

# Define the Jekyll version
#gem "jekyll", "~> 4.3" # Use the latest stable version

# Add the theme that was causing the error
gem "jekyll-theme-minimal"

# This gem is required for GitHub Pages
group :jekyll_plugins do
  # gem "jekyll-feed"
  # Add other plugins here if you use them
end

# This gem is needed if you want to use the 'github-pages' dependency group
# to mirror the GitHub Pages environment locally.
gem "github-pages", group: :jekyll_plugins